import { PagePlaceholder } from '@/components/PagePlaceholder'

export default function TendersPage() {
  return (
    <PagePlaceholder
      title="Tenders"
      description="Track active tender opportunities, deadlines, and submission status in one place."
    />
  )
}
