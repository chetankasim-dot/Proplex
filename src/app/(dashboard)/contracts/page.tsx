import { PagePlaceholder } from '@/components/PagePlaceholder'

export default function ContractsPage() {
  return (
    <PagePlaceholder
      title="Contracts"
      description="Centralized contract repository with redline review, version history, and renewal alerts."
    />
  )
}
